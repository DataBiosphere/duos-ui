version: 2

jobs:
  test:
    docker:
      - image: node:8
    steps:
      - checkout
      - restore_cache:
          key: deps1-{{ .Branch }}-{{ checksum "package.json" }}
      - run: npm install
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "package.json" }}
          paths:
            - node_modules
# TODO: Get tests to run.
#      - run: npm test
#      - persist_to_workspace:
#          root: .
#          paths:
#            - node_modules

workflows:
  version: 2
  build-deploy:
    jobs:
      - test
