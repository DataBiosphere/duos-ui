export const formattedLinkedIn = (profileId: string): string => `https://www.linkedin.com/in/${profileId}`
export const formattedOrcid = (profileId: string): string => `https://orcid.org/${profileId}`
export const formattedThroughBio = (profileId: string): string => `https://through.bio/${profileId}`
