describe('InstitutionTable', () => {
  it('renders', () => {
    // TODO: mount the component and check for basic render
  });

  it('displays paginated institution rows', () => {
    // TODO: provide mock data and verify rows are rendered
  });

  it('handles loading state', () => {
    // TODO: set isLoading prop and check for loading indicator
  });

  it('sorts columns when header is clicked', () => {
    // TODO: simulate sort interaction and verify order
  });

  it('changes page when pagination bar is used', () => {
    // TODO: interact with pagination and check page change
  });

  it('links to the update institution page', () => {
    // TODO: trigger modal open/close and verify visibility
  });
});
